---
title: Return successful Extraction responses
description: Return Media Nodes with the Source metadata Lynvo needs.
navLabel: Success responses
contentType: Reference
---

<DocSection id="success-schema">

## Return a successful Extraction response

Every successful Extraction returns Source metadata, an array of Media Nodes, and an extensions object.

<CodeBlock label="Successful response">

```json {2-7,9-15}
{
  "source": {
    "pluginServerId": "com.example.my-plugin-server",
    "displayName": "My Plugin Server",
    "pluginId": "example-media",
    "sourceName": "Example Media",
    "pageTitle": "Example collection"
  },
  "nodes": [
    {
      "kind": "playable",
      "id": "episode-1",
      "label": "Episode 1",
      "url": "https://cdn.example.com/episode-1.mp4"
    }
  ],
  "extensions": {}
}
```

</CodeBlock>

The `source` object supports:

| Field            | Required | Purpose                    |
| ---------------- | -------- | -------------------------- |
| `pluginServerId` | Yes      | Matches the manifest       |
| `displayName`    | Yes      | Plugin Server display name |
| `iconUrl`        | No       | Plugin Server icon         |
| `pluginId`       | No       | Matches a declared Plugin  |
| `sourceName`     | No       | Source display name        |
| `sourceIconUrl`  | No       | Source icon                |
| `pageTitle`      | No       | Extracted page title       |
| `audio`          | No       | Audio-language summary     |

Lynvo owns layout, watched state, saved selection, playback state, and link caching. Do not return interface instructions.

</DocSection>
