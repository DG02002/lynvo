{
  "name": "__PROJECT_NAME__",
  "version": "0.1.0",
  "private": true,
  "description": "A Lynvo-compatible Custom Plugin Server.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --config wrangler.jsonc",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "build": "wrangler deploy --dry-run --config wrangler.jsonc --env=\"\"",
    "deploy": "wrangler deploy --config wrangler.jsonc --env=\"\""
  },
  "dependencies": {
    "@lynvo/plugin-server-protocol": "^__LYNVO_PROTOCOL_VERSION__",
    "hono": "^4.12.32"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260731.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10",
    "wrangler": "^4.116.0"
  },
  "packageManager": "pnpm@10.26.1"
}
