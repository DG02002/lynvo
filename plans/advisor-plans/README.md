# Implementation Plans

Generated by the `improve` skill on 2026-08-10. Execute plans in the order
below. Each executor must read the complete plan before editing, honor its STOP
conditions, and update the status when finished.

## Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
| --- | --- | --- | --- | --- | --- |
| 001 | Make Saved links converge reliably across active sessions | P1 | L | — | BLOCKED: genuine two-browser manual acceptance unavailable in this workspace |

Status values: `TODO`, `IN PROGRESS`, `DONE`, `BLOCKED: <reason>`, or
`REJECTED: <reason>`.

## Dependency notes

- Plan 001 is deliberately one coordinated plan because the database revision,
  durable invalidation delivery, browser recovery, and contract tests form one
  correctness boundary. Landing only one layer would leave a false sense of
  realtime safety.

## Findings considered and rejected

- Restore `convex/react` directly in the browser: rejected for this plan because
  the repository intentionally keeps Convex access and refresh credentials
  behind the Worker and enforces that boundary in a test. Reconsidering that
  decision requires a separate threat-model and authorization audit of every
  public Convex function.
- One-second list polling: rejected because it is neither realtime nor
  cost-efficient and does not use the existing user realtime room.
- `BroadcastChannel` or `localStorage` events: rejected because they do not
  synchronize independent browsers or devices.
